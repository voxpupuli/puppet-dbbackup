---
version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data

hierarchy:
  - name: 'Per-node data' # Human-readable name.
    path: 'nodes/%{facts.os.release.major}.yaml' # File path, relative to datadir.

  - name: 'Common data'
    path: 'common.yaml'
